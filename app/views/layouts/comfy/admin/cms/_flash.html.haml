- flash.to_hash.except('cms_saved_draft_key').each do |type, message|
  .alert{:class => "alert-#{type}"}
    %button.close{:type => 'button', :class => "close", :data => {:dismiss => 'alert'}} &times;
    = message
